# Copy to docker/.env and adjust.
# Absolute path the data/ symlinks resolve to (`readlink -f ../data/HCP_PTN1200`).
HCP_DATA_ROOT=/home/jmbathe/Documents/1_Projects/BrainCompensation/data
# Your UID/GID, so outputs are not written as root: `id -u`, `id -g`.
HOST_UID=1002
HOST_GID=1002
CORES=8
MEM_MB=16000
